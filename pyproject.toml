[project]
name = "ScopeSim"
version = "0.5.8"
description = "Generalised telescope observation simulator"
readme = "README.md"
requires-python = ">=3.7"
license = {text = "License :: OSI Approved :: GNU General Public License v3 (GPLv3)"}
authors = [
    {name = "Kieran Leschinski", email="kieran.leschinski@unive.ac.at"},
]
maintainers = [
    {name = "Kieran Leschinski", email="kieran.leschinski@unive.ac.at"},
    {name = "Hugo Buddelmeijer", email="hugo@buddelmeijer.nl"},
]
classifiers=[
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: GNU General Public License v3 (GPLv3)",
    "Operating System :: OS Independent",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Astronomy",
]
dependencies = [
    "numpy>=1.16",
    "scipy>=1.0.0",
    "astropy>=2.0",
    "matplotlib>=1.5",

    "docutils",
    "requests>=2.20",
    "beautifulsoup4>=4.4",
    "lxml",
    "pyyaml>5.1",

    "synphot>=0.1.3",
    "skycalc_ipy>=0.1.3",
    "anisocado",
]

[project.optional-dependencies]
dev = [
    "jupyter",
    "jupytext",
]
test = [
    "pytest",
    "pytest-cov",
    "scopesim_templates",
    # Just so that readthedocs doesn't include the tests module - yes it's hacky
    "skycalc_cli",
]

[project.urls]
"Homepage" = "https://scopesim.readthedocs.io/en/latest/"
"Source" = "https://github.com/AstarVienna/ScopeSim"
"Bug Reports" = "https://github.com/AstarVienna/ScopeSim/issues"

[build-system]
# Use setuptools >= 61.0.0 for package-data
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = [
    "scopesim"
]

# TODO: remove version.py
# TODO: move long description from setup.py
# TODO: Remove setup.
# TODO: Add real licence
# TODO: Remove requirements.github_actions.txt and requirements.readthedocs.txt
